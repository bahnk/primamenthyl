[project]
name = "primamethyl-dbt-models"
version = "0.1.0"
description = "dbt models for primamethyl DuckDB outputs."
readme = "README.md"
requires-python = ">=3.11,<3.14"
dependencies = [
  "dbt-duckdb>=1.9,<2.0",
]

[tool.uv]
package = false
