[project]
name = "primamenthyl-models"
version = "0.1.0"
description = "Model training scripts for primamenthyl merged DuckDB outputs."
requires-python = ">=3.11,<3.14"
dependencies = [
  "duckdb",
  "matplotlib",
  "pandas",
  "scikit-learn",
]

[tool.uv]
package = false
